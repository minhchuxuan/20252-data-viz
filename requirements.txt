# Paris 2024 Olympic Data Story -- Python dependencies.
#
# Pins below are the versions actually verified working on this machine
# (Python 3.11, conda env `dataviz261` / the reused `mpg` env). They replace
# the original Python-3.13-incompatible pins (streamlit 1.32 / pandas 2.2 /
# numpy 1.26 / plotly 5.19), which lacked 3.13 wheels.
#
# Newer minors also work; loosen to `>=` if you need to. kaleido is required
# for static figure export (scripts/make_figures.py) and drives a headless
# Chrome -- see USAGE.md for the one-time Chrome + system-library setup.

streamlit==1.58.0
pandas==3.0.1
numpy==2.4.3
plotly==6.8.0
kaleido==1.3.0
